## Zarf package for KubeVirt and CDI

### Description

This folder and associated Zarf package contains two Zarf components:
* `kubevirt` and
* `cdi`

These components are intended to be used as sub-components to the `dco-core` parent package.
